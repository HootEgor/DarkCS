---

env: dev
telegram:
  api_key: ${TELEGRAM_API_KEY}
  admin_id:
    # egor
    - 653782769
    # ruslan
    - 64156944
  bot_name: Dark Logger
  enabled: true
openai:
  api_key: ${OPENAI_API_KEY}
  overseer_id: ${OVERSEER_ID}
  consultant_id: ${CONSULTANT_ID}
  calculator_id: ${CALCULATOR_ID}
  dev_prefix: Dev
save_path: /var/lib/darkcs
mongo:
  enabled: true
  host: ${MONGO_HOST}
  port: ${MONGO_PORT}
  user: ${MONGO_USER}
  password: ${MONGO_PASSWORD}
  database: ${MONGO_DATABASE}
  expired_days: 7
prod-service:
  login: ${REPO_LOGIN}
  password: ${REPO_PASSWORD}
  base_url: ${REPO_BASE_URL}
smart-sender:
  api_key: ${SMART_API_KEY}
  send_msg_url: https://api.smartsender.com/v1
listen:
  bind_ip: 0.0.0.0
  port: ${LISTEN_PORT}
  key: ${API_KEY}
